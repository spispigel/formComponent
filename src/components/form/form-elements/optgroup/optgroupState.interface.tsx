export interface IOptgroupState { 
 /**
   * If true, component will be disabled.
   * @default false
   */
  disabled?: boolean;
  /**
   * Label for the option group element.
   */
  label?: string;

}